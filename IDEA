# create: 2010-02-23

想做一個東西
就是讓系統還原的速度加快
也可以說是備份，然後自動安裝等等的動作

備份功能列表如下
1.應用程式備份回復(每週做一次，只留最新二份)
2.設定檔(可能每週做一次，只留最新三份)
3.資料庫(每天做一次，留最新一個月)
4.hardcode(每天做一次，留最新一個星期)

備份應該會用scp的方式
而且是主機

打包功能列表如下
1.應用程式安裝流程(這個我本來就有範本可以參考了)
2.將備份功能裡，最新日期的打包進來
